function sendEmail(email: string, subject: string, body: string){


    // send email to email with subject and body
    return `Email sent to ${email} with subject ${subject} and body ${body}`;
}

export {sendEmail};

// name = TemplateView
// pass = ritx ebjy jdmw rovk
